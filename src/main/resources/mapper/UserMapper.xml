<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.springmvc.dao.UserDao">
    <!--auto generated Code-->
    <resultMap id="AllColumnMap" type="cn.springmvc.entry.User">
        <result column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="uname" property="uname"/>
        <result column="password" property="password"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="identity" property="identity"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        `id`,
        `email`,
        `uname`,
        `password`,
        `description`,
        `status`,
        `identity`
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO user (
            `id`,
            `email`,
            `uname`,
            `password`,
            `description`,
            `status`,
            `identity`
        ) VALUES (
            #{user.id},
            #{user.email},
            #{user.uname},
            #{user.password},
            #{user.description},
            #{user.status},
            #{user.identity}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null"> `id`,</if>
            <if test="user.email!=null"> `email`,</if>
            <if test="user.uname!=null"> `uname`,</if>
            <if test="user.password!=null"> `password`,</if>
            <if test="user.description!=null"> `description`,</if>
            <if test="user.status!=null"> `status`,</if>
            <if test="user.identity!=null"> `identity`,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null">#{user.id},</if>
            <if test="user.email!=null">#{user.email},</if>
            <if test="user.uname!=null">#{user.uname},</if>
            <if test="user.password!=null">#{user.password},</if>
            <if test="user.description!=null">#{user.description},</if>
            <if test="user.status!=null">#{user.status},</if>
            <if test="user.identity!=null">#{user.identity},</if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO user (
        <include refid="all_column"/>
        )VALUES
        <foreach collection="users" item="user" index="index" separator=",">
            (
            #{user.id},
            #{user.email},
            #{user.uname},
            #{user.password},
            #{user.description},
            #{user.status},
            #{user.identity}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE user
        <set>
            <if test="user.id != null"> `id` = #{user.id}, </if>
            <if test="user.email != null"> `email` = #{user.email}, </if>
            <if test="user.uname != null"> `uname` = #{user.uname}, </if>
            <if test="user.password != null"> `password` = #{user.password}, </if>
            <if test="user.description != null"> `description` = #{user.description}, </if>
            <if test="user.status != null"> `status` = #{user.status}, </if>
            <if test="user.identity != null"> `identity` = #{user.identity} </if>
        </set>
        WHERE id = #{user.id}
    </update>


<!--auto generated by codehelper on 2017-05-04 17:32:24-->
    <select id="findEmailByEmail" resultType="java.lang.String">
        select `email`
        from user
        where `email`=#{email}
    </select>

<!--auto generated by codehelper on 2017-05-04 17:33:14-->
    <select id="findByEmailAndPassword" resultMap="AllColumnMap">
        select
        <include refid="all_column"/>
        from user
        where `email`=#{email} and `password`=#{password}
    </select>


</mapper>

